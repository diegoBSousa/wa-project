import '../../enviroment';
import 'regenerator-runtime/runtime';

const request = require('supertest');
const app = require('../../src/app');

describe('Login', () => {
  it('foo', () => {
    const x = 5;
    expect(x).toBe(5);
  });
});
